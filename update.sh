git status  # Check which files changed
git commit -m "Update deployment scripts and add lh directory"
git push origin main
git add .  # Stage all changes
git commit -m "Refactored build scripts and updated documentation"
git push origin main  # Push changes to the main branch on the remote

